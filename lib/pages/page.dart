import 'package:flutter/material.dart';

class LostAndFound extends StatelessWidget {
  const LostAndFound({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LostAndFound',style: TextStyle(fontSize: 60),),
      ),
    );
  }
}

class Competition extends StatelessWidget {
  const Competition({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Competition',style: TextStyle(fontSize: 60),),
      ),
    );
  }
}
class FriendsFeed extends StatelessWidget {
  const FriendsFeed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FriendsFeed',style: TextStyle(fontSize: 60),),
      ),
    );
  }
}
class VoiceOut extends StatelessWidget {
  const VoiceOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('VoiceOut',style: TextStyle(fontSize: 60),),
      ),
    );
  }
}
class Placements extends StatelessWidget {
  const Placements({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Placements',style: TextStyle(fontSize: 60),),
      ),
    );
  }
}
class ComingSoon extends StatelessWidget {
  const ComingSoon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ComingSoon',style: TextStyle(fontSize: 60),),
      ),
    );
  }
}
 class ProfileSettings extends StatelessWidget {
   const ProfileSettings({Key? key}) : super(key: key);

   @override
   Widget build(BuildContext context) {
     return Scaffold(
       body: Center(
         child: Text('ProfileSettings',style: TextStyle(fontSize: 60),),
       ),
     );
   }
 }


